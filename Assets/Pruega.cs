using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pruega : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Añadasd");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
